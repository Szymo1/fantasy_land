import { MapBackground } from "../../backgrounds/MapBackground/MapBackground"
import { MapTemplate } from "../../templates/MapTemplate/MapTemplate"

export const Map = () => {
    return(
        <MapBackground>
            <MapTemplate />
        </MapBackground>
    )
}